import { h } from "preact";

export default function NotFound()
{
    return(
        <div class="container-fluid">
            <div class="row">
                <h1>Oops...</h1>
            </div>
        </div>
    );
}